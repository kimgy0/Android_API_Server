server:
  port: 2000

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://database-androidproject.csvqw3h5cfio.ap-northeast-2.rds.amazonaws.com:3306/db
    username: admin
    password: c9865974

  jpa:
    hibernate:
      ddl-auto: create
    properties:

      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect
        format_sql: true
      # show_sql: true

  logging.level:
    org.hibernate.SQL: debug

file:
  dir: /home/ubuntu/Android_API_Server/src/main/resources/

